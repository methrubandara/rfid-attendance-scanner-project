<?php

  //include_once 'backend/database.php';
  //include_once 'backend/utility.php';

  $json = file_get_contents('php://input');
  //$data = json_decode($json);
  
  //$id = (integer) "$data->data";
  //$device = "$data->coreid";
  //$time= "$data->published_at";

  //runQuery([(integer) $id, $time, $device], 'INSERT INTO `scans_log`(`card_id`, `time_stamp`, `device_id`) VALUES (?,?,?)');

  //echo json_encode(array(
    //"allow" => "TRUE",
    //"Time_Stamp" => "$data->published_at",
    //"data" => "$data",
  //));

  //trying to get the scanners to work....
  //those were the days
  echo "God is confued and weirded out";

    
?>
